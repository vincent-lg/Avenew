"""
Package containing the variuos parameters used by the SmartCommand system.

The 'pipe' module is an exception, as it contains the ParamPipe
class, which is used systematically by SmartCommand, although
you should probably never use it directly, except to add new
parameters to a command.

"""
